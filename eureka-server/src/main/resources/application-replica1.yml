server:
  port: 8002
spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: replica1
  client:
    service-url:
      defaultZone: http://replica2:8003/eureka/
    fetch-registry: true
    register-with-eureka: true